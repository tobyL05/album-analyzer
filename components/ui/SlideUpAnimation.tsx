"use client"

import { useEffect } from "react"

export default function ServerComponentAnimation({ children } : { children: React.ReactNode}) {
    
    useEffect(() => {
        
    },[])

    // animate slide up
    return (
        <>
            { children } 
        </>
    )

}